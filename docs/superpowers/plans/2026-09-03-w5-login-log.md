# 登录日志模块（W5-2）实现计划

> **面向执行者的说明：** 本计划沿用 W5-1 协作约定：AI 负责脚手架、域定义、
> 失败测试、最终 review 与验证；业务实现默认由用户手动完成（执行中可逐任务
> 改授权给 AI）。步骤用 **[AI]** / **[用户]** 标注，复选框跟踪进度。
> 不使用 subagent-driven-development（项目多代理模式未启用）。

**目标：** 登录成功 / 失败自动落库（登录 service 内写，msg 内部分级），
管理端提供只读分页 / 详情 / 单删 / 批量删除（软删），并补菜单与权限码种子。

**架构：** 新增 `login_log` 域（codegen 产出，无唯一字段，验证 W5-1 模板修复）；
`auth::service::login` 增加 `LoginMeta { ip, agent }` 入参并在分支内写日志，
对外提示统一防枚举；管理端挂 `AuthRequired + OperationLog`。

**技术栈：** Salvo 0.95.2、SeaORM 1.x、chrono、codegen 生成器。

**规格依据：** `docs/superpowers/specs/2026-09-03-w5-login-log-design.md`
（commit e3f7e18）

---

## 文件结构

创建：
- `migrations/src/m20260903_000008_create_sys_login_log.rs`
- `codegen/defs/login_log.json`
- `src/entity/sys_login_log.rs`（codegen 生成）
- `src/modules/login_log/{mod,api,dto,repo,service}.rs`（codegen 生成后裁剪）

修改：
- `migrations/src/lib.rs`
- `src/entity/mod.rs`、`src/modules/mod.rs`
- `src/infra/router.rs`（挂载 login-log）
- `src/modules/auth/{dto,api,service}.rs`（LoginMeta、防枚举、落库、Request 取 IP/UA）
- `src/infra/seed.rs`（菜单与权限码）
- `docs/Rust学习打卡记录.md`

测试写入点：
- `src/modules/login_log/repo.rs`：过滤 / 倒序 / 软删 / 批量软删
- `src/modules/login_log/service.rs`：page / get / delete / delete-batch
- `src/modules/auth/service.rs`：登录成功与失败分支的日志断言、对外文案统一
- `src/infra/seed.rs`：登录日志菜单与按钮断言

---

## 任务 1：sys_login_log 迁移（[AI]）

**文件：**
- 创建：`migrations/src/m20260903_000008_create_sys_login_log.rs`
- 修改：`migrations/src/lib.rs`

- [ ] **步骤 1：编写迁移**

参照 W5-1 的 `m20260903_000007_create_sys_operation_log.rs` 结构，表字段按
规格第 3 节，关键差异如下（完整代码落盘时由 AI 补齐）：

```rust
enum SysLoginLog {
    Table, Id, UserId, Username, Ip, Agent, Status, Msg,
    CreatedAt, UpdatedAt, DeletedAt,
}
// up() 创建表：
// id BIGINT UNSIGNED PK AUTO_INCREMENT
// user_id BIGINT UNSIGNED NOT NULL DEFAULT 0
// username VARCHAR(64) NOT NULL
// ip VARCHAR(64) NOT NULL DEFAULT ''
// agent VARCHAR(255) NOT NULL DEFAULT ''
// status TINYINT NOT NULL DEFAULT 0
// msg VARCHAR(255) NOT NULL DEFAULT ''
// created_at / updated_at / deleted_at 同 W5-1
// 索引：idx_sys_login_log_created_at、idx_sys_login_log_username
```

- [ ] **步骤 2：注册迁移并应用**

`migrations/src/lib.rs` 追加
`mod m20260903_000008_create_sys_login_log;` 与 Migrator 列表项。

```bash
cd migrations && DATABASE_URL='mysql://root:root@localhost:3307/salvo_vben' cargo run -- up
```

- [ ] **步骤 3：Commit（[用户]）**

```bash
git add migrations/src/m20260903_000008_create_sys_login_log.rs migrations/src/lib.rs
git commit -m "feat(migrations): 新增 sys_login_log 登录日志表"
```

---

## 任务 2：域定义 + codegen 骨架（[AI]）

**文件：**
- 创建：`codegen/defs/login_log.json`
- 创建：`src/entity/sys_login_log.rs`、`src/modules/login_log/*.rs`（生成器输出）
- 修改：`src/entity/mod.rs`、`src/modules/mod.rs`

- [ ] **步骤 1：域定义 JSON**

```json
{
  "domain": "login_log",
  "table": "sys_login_log",
  "comment": "登录日志",
  "fields": [
    { "name": "id", "rust_type": "u64", "sql_type": "BIGINT UNSIGNED", "primary": true, "auto_increment": true },
    { "name": "user_id", "rust_type": "u64", "sql_type": "BIGINT UNSIGNED", "optional": true, "default": 0, "comment": "登录成功后的用户 ID，失败为 0" },
    { "name": "username", "rust_type": "String", "sql_type": "VARCHAR(64)", "optional": false, "comment": "本次尝试的用户名" },
    { "name": "ip", "rust_type": "String", "sql_type": "VARCHAR(64)", "optional": true, "default": "", "comment": "来源 IP" },
    { "name": "agent", "rust_type": "String", "sql_type": "VARCHAR(255)", "optional": true, "default": "", "comment": "User-Agent" },
    { "name": "status", "rust_type": "i8", "sql_type": "TINYINT", "optional": true, "default": 0, "comment": "1 成功 / 0 失败" },
    { "name": "msg", "rust_type": "String", "sql_type": "VARCHAR(255)", "optional": true, "default": "", "comment": "内部原因或成功提示" },
    { "name": "created_at", "rust_type": "DateTime", "sql_type": "DATETIME", "readonly": true },
    { "name": "updated_at", "rust_type": "DateTime", "sql_type": "DATETIME", "readonly": true },
    { "name": "deleted_at", "rust_type": "Option<DateTime>", "sql_type": "DATETIME", "readonly": true, "soft_delete": true }
  ],
  "unique_fields": [],
  "filters": [
    { "field": "username", "kind": "keyword" },
    { "field": "ip", "kind": "keyword" },
    { "field": "status", "kind": "exact" }
  ]
}
```

- [ ] **步骤 2：运行 codegen**

```bash
cd codegen && cargo run -- generate ../codegen/defs/login_log.json
```

预期直接成功且无语法坏参数（验证 W5-1 模板修复）；随后手工注册
`src/entity/mod.rs` / `src/modules/mod.rs`。

- [ ] **步骤 3：验证编译**

```bash
cargo check
```

若再出现 `fn create_req(: String)` 说明模板修复不完整，立即停下回修
`codegen/src/templates.rs`（不应手工绕过）。

- [ ] **步骤 4：Commit（[用户]）**

```bash
git add codegen/defs/login_log.json src/entity/sys_login_log.rs src/modules/login_log src/entity/mod.rs src/modules/mod.rs
git commit -m "feat(codegen): 用生成器产出登录日志域骨架（验证无唯一域模板）"
```

---

## 任务 3：login_log repo + DTO + 管理 service（[AI] 测试 → [用户] 实现）

**文件：** `src/modules/login_log/{repo,dto,service}.rs`

- [ ] **步骤 1：AI 写失败测试**

repo 测试替换生成测试段，用例：

```rust
// seed：直接 Entity insert，username/ip 用 unique("repo_*") 前缀
#[tokio::test]
async fn find_page_filters_by_username_ip_status_sorts_desc_excludes_deleted() {
    // 三条活记录 + 一条软删：
    // username 命中 + ip 命中 + status=1 命中 → 断言过滤与倒序
}

#[tokio::test]
async fn soft_delete_batch_only_marks_alive_rows() {
    // 两条活 + 一条已软删：affected == 2，查询不可见
}
```

service 测试用例：

```rust
// get / delete 不存在 → Biz("登录日志不存在：{id}")
// delete_operation_log_batch 空数组返回 0；混合 id 只删活行
```

- [ ] **步骤 2：运行确认红（[用户]）**

```bash
cargo test login_log 2>&1 | tail -20
```

- [ ] **步骤 3：用户裁剪 dto.rs**

保留生成 Resp（全字段）并补 created_at：

```rust
#[derive(Debug, Serialize, ToSchema)]
pub struct LoginLogResp {
    pub id: u64,
    pub user_id: u64,
    pub username: String,
    pub ip: String,
    pub agent: String,
    pub status: i8,
    pub msg: String,
    pub created_at: String, // format!("{}", m.created_at)
}

impl From<sys_login_log::Model> for LoginLogResp { /* 同字段 */ }

// 删除生成的 CreateLoginLogReq / UpdateLoginLogReq；
// ListReq / Filter 字段：username: Option<String>、ip: Option<String>、
//   status: Option<i8>（内嵌 PageQuery）
#[derive(Debug, Deserialize, ToSchema)]
pub struct DeleteBatchReq { pub ids: Vec<u64> }
```

- [ ] **步骤 4：用户实现 repo.rs**

删除 `update_login_log`，保留 `create_login_log`（登录 service 落库入口），
`find_page` 增加双模糊与倒序：

```rust
pub async fn find_page(
    db: &DatabaseConnection,
    filter: &LoginLogFilter,
    page_index: u64,
    page_size: u64,
) -> anyhow::Result<crate::utils::PageData<Model>> {
    let mut cond = Condition::all();
    if let Some(username) = &filter.username {
        cond = cond.add(sys_login_log::Column::Username.like(format!("%{username}%")));
    }
    if let Some(ip) = &filter.ip {
        cond = cond.add(sys_login_log::Column::Ip.like(format!("%{ip}%")));
    }
    if let Some(status) = filter.status {
        cond = cond.add(sys_login_log::Column::Status.eq(status));
    }
    let select = sys_login_log::Entity::find()
        .filter(cond)
        .filter(sys_login_log::Column::DeletedAt.is_null())
        .order_by_desc(sys_login_log::Column::CreatedAt);
    crate::utils::paginate(select, db, page_index, page_size).await
}

pub async fn create_login_log(
    db: &DatabaseConnection,
    model: sys_login_log::ActiveModel,
) -> anyhow::Result<Model> {
    Ok(model.insert(db).await?)
}

// soft_delete_login_log 单条；soft_delete_batch 批量（含 DeletedAt.is_null 过滤）
```

- [ ] **步骤 5：用户实现 service.rs**

删除生成 create / update，保留并调整：

```rust
pub async fn page_login_logs(
    db: &DatabaseConnection,
    req: &LoginLogListReq,
) -> Result<PageData<sys_login_log::Model>, AppError> {
    // 组装 LoginLogFilter 后调 repo::find_page
}

pub async fn get_login_log(db: &DatabaseConnection, id: u64)
    -> Result<sys_login_log::Model, AppError> {
    // 不存在 → Biz("登录日志不存在：{id}")
}

pub async fn delete_login_log(db: &DatabaseConnection, id: u64)
    -> Result<(), AppError> {
    // 不存在 → Biz；存在 → repo::soft_delete_login_log
}

pub async fn delete_login_log_batch(
    db: &DatabaseConnection,
    ids: &[u64],
) -> Result<u64, AppError> {
    if ids.is_empty() {
        return Ok(0);
    }
    Ok(repo::soft_delete_batch(db, ids).await?)
}
```

- [ ] **步骤 6：运行测试验证绿（[用户]）**

```bash
cargo test login_log 2>&1 | tail -20
```

- [ ] **步骤 7：Commit（[用户]）**

```bash
git add src/modules/login_log/repo.rs src/modules/login_log/dto.rs src/modules/login_log/service.rs
git commit -m "feat(login-log): 分页查询与软删数据契约"
```

---

## 任务 4：管理 api / mod / router 挂载（[用户]）

**文件：** `src/modules/login_log/{api,mod}.rs`、`src/infra/router.rs`

- [ ] **步骤 1：裁剪 api.rs**

只保留 list / get / delete / delete-batch：

```rust
pub async fn list_login_logs(depot: &mut Depot, body: JsonBody<LoginLogListReq>)
    -> ApiResult<PageResult<LoginLogResp>>;
pub async fn get_login_log(depot: &mut Depot, body: JsonBody<IdReq>)
    -> ApiResult<LoginLogResp>;
pub async fn delete_login_log(depot: &mut Depot, body: JsonBody<IdReq>)
    -> ApiResult<()>;
pub async fn delete_login_log_batch(depot: &mut Depot, body: JsonBody<DeleteBatchReq>)
    -> ApiResult<u64>;
```

handler 内部照 W5-1 模式：`AppState::from_depot` → service。

- [ ] **步骤 2：裁剪 mod.rs**

```rust
pub fn routes() -> Router {
    Router::with_path("login-log")
        .oapi_tags(["登录日志"])
        .push(Router::with_path("list").post(api::list_login_logs))
        .push(Router::with_path("get").post(api::get_login_log))
        .push(Router::with_path("delete").post(api::delete_login_log))
        .push(Router::with_path("delete-batch").post(api::delete_login_log_batch))
}
```

- [ ] **步骤 3：router.rs 挂载**

在 operation-log 块后追加：

```rust
.push(
    Router::with_path("login-log")
        .hoop(AuthRequired)
        .hoop(OperationLog)
        .push(crate::modules::login_log::routes()),
)
```

- [ ] **步骤 4：验证**

```bash
cargo check
cargo test login_log 2>&1 | tail -10
```

- [ ] **步骤 5：Commit（[用户]）**

```bash
git add src/modules/login_log/api.rs src/modules/login_log/mod.rs src/infra/router.rs
git commit -m "feat(login-log): 挂载只读管理端点"
```

---

## 任务 5：登录落库与防枚举（[AI] 测试 → [用户] 实现，核心）

**文件：** `src/modules/auth/{dto,api,service}.rs`

- [ ] **步骤 1：AI 更新失败测试**

把 auth service 现有登录测试的 `login(...)` 调用全部补上
`LoginMeta { ip: "127.0.0.1".into(), agent: "test".into() }`，并新增断言：

```rust
#[tokio::test]
async fn login_success_writes_login_log() {
    // 登录成功后按 username 查 sys_login_log：
    // status == 1、user_id == 用户 id、msg == "登录成功"
}

#[tokio::test]
async fn login_failures_are_distinguished_in_log_but_unified_outside() {
    // 不存在用户 / 密码错误 / 禁用用户：
    // 1) 表内各产生 status=0 且 msg 分别为
    //    "用户不存在" / "密码错误" / "用户已被禁用"；
    // 2) 对外 Err(AppError::Biz) 的 message 全部为 "用户名或密码错误"
    // 3) user_id == 0
}
```

注意：登录失败分支会向 sys_login_log 写行，测试结束后按 id 物理清理；
username 用 `unique_name()` 避免相互污染。

- [ ] **步骤 2：运行确认红（[用户]）**

```bash
cargo test auth::service 2>&1 | tail -20
```

预期：编译失败（LoginMeta 不存在 / login 签名变化）。

- [ ] **步骤 3：用户改造 dto.rs**

```rust
/// 登录请求的客户端元信息（由 handler 从 Request 提取）。
#[derive(Debug, Clone, Default)]
pub struct LoginMeta {
    pub ip: String,
    pub agent: String,
}
```

- [ ] **步骤 4：用户改造 service.rs**

签名与分支逻辑（骨架，实现时保留现有测试的辅助函数）：

```rust
pub async fn login(
    db: &DatabaseConnection,
    jwt_cfg: &JwtConfig,
    req: LoginReq,
    meta: LoginMeta,
) -> Result<LoginResp, AppError> {
    let user = match user_repo::find_by_username(db, &req.username).await {
        Ok(Some(user)) => user,
        Ok(None) => {
            record_login(db, &req, 0, 0, "用户不存在", &meta).await;
            return Err(AppError::Biz("用户名或密码错误".into()));
        }
        Err(err) => {
            record_login(db, &req, 0, 0, "查询用户失败", &meta).await;
            return Err(err.into());
        }
    };

    if !crypt::verify_password(&req.password, &user.password) {
        record_login(db, &req, user.id, 0, "密码错误", &meta).await;
        return Err(AppError::Biz("用户名或密码错误".into()));
    }
    if user.status != 1 {
        record_login(db, &req, user.id, 0, "用户已被禁用", &meta).await;
        return Err(AppError::Biz("用户名或密码错误".into()));
    }

    let roles = user_repo::find_roles_by_user_id(db, user.id).await?;
    let role_keys: Vec<String> = roles.into_iter().map(|r| r.role_key).collect();
    match jwt::sign(
        user.id,
        &user.username,
        &role_keys,
        &jwt_cfg.secret,
        jwt_cfg.ttl_seconds,
    ) {
        Ok(token) => {
            record_login(db, &req, user.id, 1, "登录成功", &meta).await;
            Ok(LoginResp { token })
        }
        Err(err) => {
            record_login(db, &req, user.id, 0, "token 签发失败", &meta).await;
            Err(err.into())
        }
    }
}

/// 登录日志落库（失败只降级，不影响登录结果）。
async fn record_login(
    db: &DatabaseConnection,
    req: &LoginReq,
    user_id: u64,
    status: i8,
    msg: &str,
    meta: &LoginMeta,
) {
    let model = sys_login_log::ActiveModel {
        user_id: Set(user_id),
        username: Set(truncate_chars(&req.username, 64)),
        ip: Set(truncate_chars(&meta.ip, 64)),
        agent: Set(truncate_chars(&meta.agent, 255)),
        status: Set(status),
        msg: Set(truncate_chars(msg, 255)),
        ..Default::default()
    };
    if let Err(err) =
        crate::modules::login_log::repo::create_login_log(db, model).await
    {
        tracing::error!(%err, "login log 落库失败");
    }
}

fn truncate_chars(s: &str, max: usize) -> String {
    s.chars().take(max).collect()
}
```

注意：`record_login` 需要 import `sys_login_log` 与
`crate::modules::login_log::repo as login_log_repo`；模块间引用方向
`auth → login_log` 单向无环。

- [ ] **步骤 5：用户改造 api.rs**

`login` handler 增加 Request 并构造 meta（IP / UA 均截断）：

```rust
#[endpoint]
pub async fn login(
    depot: &mut Depot,
    req: &Request,
    body: JsonBody<LoginReq>,
) -> ApiResult<LoginResp> {
    let state = AppState::from_depot(depot)?;
    let meta = LoginMeta {
        ip: req.remote_addr().ip().map(|a| a.to_string()).unwrap_or_default(),
        agent: req
            .headers()
            .get(salvo::http::header::USER_AGENT)
            .and_then(|v| v.to_str().ok())
            .unwrap_or_default()
            .to_string(),
    };
    let resp = auth_service::login(&state.db, &state.config.jwt, body.into_inner(), meta).await?;
    Ok(ApiResponse::ok(resp))
}
```

- [ ] **步骤 6：运行测试验证绿（[用户]）**

```bash
cargo test auth::service 2>&1 | tail -20
cargo test login_log 2>&1 | tail -10
```

- [ ] **步骤 7：Commit（[用户]）**

```bash
git add src/modules/auth/dto.rs src/modules/auth/api.rs src/modules/auth/service.rs
git commit -m "feat(login-log): 登录成功失败自动落库并对内分级、对外防枚举"
```

---

## 任务 6：菜单与权限码种子（[AI] 测试 → [用户] 实现）

**文件：** `src/infra/seed.rs`

- [ ] **步骤 1：AI 追加失败测试**

```rust
#[tokio::test]
async fn ensure_seed_creates_login_log_menu_and_button() {
    // SystemLoginLog 页面存在（title=登录日志、menu_type=2、parent=System）；
    // system:login-log:delete 按钮存在且 parent_id 指向页面
}
```

- [ ] **步骤 2：运行确认红（[用户]）**

```bash
cargo test infra::seed 2>&1 | tail -15
```

- [ ] **步骤 3：用户追加 MENU_SEEDS**

```rust
    // 登录日志页面 + 删除权限码（W5-2）
    MenuSeed {
        name: "SystemLoginLog",
        title: "登录日志",
        path: "/system/login-log",
        component: "#/views/system/login-log/index.vue",
        icon: "lucide:history",
        menu_type: 2,
        permission: "",
        parent: Some("System"),
        sort: 6,
    },
    MenuSeed {
        name: "SystemLoginLogDelete",
        title: "登录日志删除",
        path: "",
        component: "",
        icon: "",
        menu_type: 3,
        permission: "system:login-log:delete",
        parent: Some("SystemLoginLog"),
        sort: 1,
    },
```

- [ ] **步骤 4：运行测试验证绿（[用户]）**

```bash
cargo test infra::seed 2>&1 | tail -10
```

- [ ] **步骤 5：Commit（[用户]）**

```bash
git add src/infra/seed.rs
git commit -m "feat(login-log): 种子菜单与删除权限码"
```

---

## 任务 7：全量验证 + Review + 打卡（[AI]）

- [ ] **步骤 1：全量验证**

```bash
cargo fmt --check
cargo check
cargo test 2>&1 | tail -10
cd codegen && cargo test 2>&1 | tail -5
```

预期：双项目全绿；若遇到既有并发互扰测试（如 user 域），单跑确认后记录。

- [ ] **步骤 2：Review**

按规格核对：msg 分级与对外统一文案、禁用分支合并后无泄漏、user_id 成功有值
失败为 0、字段截断、软删口径、接口顺序与命名、菜单 sort 顺序、codegen 无唯一
域模板未再手工绕过。

- [ ] **步骤 3：更新打卡记录（[AI]）**

追加 2026-09-03 W5-2 登录日志条目（含测试数与提交）。

- [ ] **步骤 4：Commit（[用户]）**

```bash
git add docs/Rust学习打卡记录.md
git commit -m "docs: 2026-09-03 W5-2 登录日志打卡"
```

---

## 自检结论

- 规格覆盖：表迁移（任务 1）、codegen 域（任务 2）、管理端数据契约与接口
  （任务 3/4）、登录写入与防枚举（任务 5）、种子（任务 6）、验证与打卡
  （任务 7）；
- 无占位符：各实现步骤均给签名或可编译骨架；测试由 AI 在步骤中落盘；
- 类型一致：表/实体 `sys_login_log`；域 `login_log`；URL `login-log`；
  DTO `LoginLogResp` / `LoginLogListReq` / `LoginLogFilter` / `DeleteBatchReq`；
  auth 侧 `LoginMeta` 与 `login(db, jwt_cfg, req, meta)` 签名全计划一致。
