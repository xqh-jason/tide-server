# W6-2 定时任务模块实现计划

> **面向 AI 代理的工作者：** 遵循协作约定——AI 交付依赖、迁移、codegen 产出、骨架
> （签名 + `todo!()` + 步骤注释）与失败测试；用户手写业务实现；AI review 收尾。
> 规格依据：`docs/superpowers/specs/2026-09-07-w6-job-design.md`

---

## 当前进度快照（2026-09-07 · 骨架交接，供新会话接续）

- **红灯状态**：`cargo test` = 213 通过 / 4 预期红（4 个调度接线测试；
  `find_active_jobs`、`validate_cron_expr` 用户已完成转绿）；
  警告全部是 `todo!()`/待接线标记，实现后逐批消失。
- **骨架调整（2026-09-07 晚）**：service 四个接线函数签名 `&JobScheduler` →
  `&AppState`（构造调度 Job 的闭包必须捕获 `Arc<AppState>`）；scheduler 补两个完整
  helper `register_job` / `unregister_job`（build+add / remove，内部统一失败降级
  error 日志）；测试探测 `next_tick_for_job` 需要 `&mut`，改用 `scheduler.clone()`
  共享句柄（`is_registered`）。
- **用户实现清单（按序）**：
  1. `job/scheduler.rs` → `build_scheduled_job`（按函数内 todo 提示的三元组闭包写法；
     `Job::new_async` 是同步函数，两参闭包返回 `Box::pin` future，无需 await/runtime）
  2. `job/scheduler.rs` → `run_scheduled`（`running_flags()` 防重叠）+
     `execute_job_run`（timeout 300s 包裹 + 写 `sys_job_log`，error_msg 截断 2KB）
  3. `src/task/login_log_cleanup.rs` / `job_log_cleanup.rs` → 两个 `run`
     （调各域 repo 的 `delete_created_before`，repo 函数已实现）
  4. `job/service.rs` → create/update/delete/update-status 四组调度接线
     （helper 一行式调用；删除 4 行「骨架期未使用」注释）
- **实现完成后 [AI]**：review（事务边界 / 防重叠 / panic 落库路径）→ 全量验证
  （fmt / check all-targets / 双 crate test / doc 0 警告）→ 冒烟四连
  （启用任务持续落 job_log → 改表达式实时生效 → 停用移除 → 删除无残留）→
  commit `feat(job): 定时任务调度与执行日志（W6-2）` → 打卡。
- **已定稿设计**：spec §4~§5；handler 注册表在 `src/task/`（一个任务一个文件，
  新增任务 = 建文件 + 注册一行）。

---

## 任务 1：依赖与基础设施（[AI]）

- [x] Cargo.toml 引入 `tokio-cron-scheduler = "0.15.1"`（先 dry-run 验证 rsproxy）
- [x] `AppState` 增加 `scheduler: Arc<JobScheduler>`；`app.rs` 在 seed 后调用
  `job::scheduler::init_scheduler(&state)`（内部调 `find_active_jobs`——实现完成前
  cargo run 在此 panic 属预期红线状态）
- [x] 验证镜像可用性，缺失则记入避坑清单

## 任务 2：迁移与主数据（[AI]）

- [x] `m20260907_000013_create_sys_job_and_sys_job_log`（字段/索引/中文注释，down 可回滚），注册 lib.rs
- [x] `codegen/defs/job.json`（audit 字段）+ `job_log.json`；运行生成器产出 entity + 四件套
- [x] entity 注册（entity/mod.rs），生成物按任务 3 裁剪

## 任务 3：骨架与失败测试（[AI]）

- [x] `src/modules/job/scheduler.rs` 骨架（纯基础设施）
- [x] `src/task/`：`mod.rs` 注册表 + `login_log_cleanup.rs` / `job_log_cleanup.rs`
  （handler 一个任务一个文件；两域 repo 补 `delete_created_before`）：`init_scheduler`（完整基建代码，调用户 `todo!()` 函数）、
  `job_runner`（`todo!()`：防重叠 + timeout + 落日志）、`run_job_now`（`todo!()`）、
  `HANDLERS` 注册表（`todo!()`：两个内置清理 handler）
- [x] job 域 service：CRUD 调度接线 `todo!()`（add/remove 同步、handler_name 注册表校验、
  update-status / run-once 端点）；job_log 域裁剪为只读 + 删除（对齐 login_log）
- [x] 路由挂载 `/job`、`/job-log`（三件套中间件）；种子：菜单 + 按钮权限码 + 启用示例任务
- [x] 失败测试：
  - repo：`find_by_name_include_deleted` 查重含软删、`find_active_jobs` 只含启用未删、
    update-status 翻转、分页审计过滤
  - service：create 成功后调度器含任务（uuid=派生）、未知 handler 拒绝、
    delete 后 remove、update-status 翻转接线
  - infra green：调度 smoke（每秒任务写 cache，2.5s 窗口）
- [x] 确认红灯：编译通过、新测试红、既有测试绿

## 任务 4：业务实现（[用户]，依次完成）

- [x] repo：`find_by_name_include_deleted` / `find_active_jobs` / 其余查询
- [x] handler 注册表（落位 `src/task/mod.rs`）+ 两个内置清理 handler
- [x] `job_runner` 系列（防重叠 entry 占坑 + timeout + 日志落库；构建用
  `JobBuilder::with_job_id` 固定派生 uuid——new_async 内部 random uuid 对不回 job_id）
- [x] service CRUD ↔ scheduler 接线（create/update/update-status/delete/run-once；
  签名 `&JobScheduler` → `&AppState`，helper `register_job`/`unregister_job` 统一降级日志）
- [x] **[AI]** 每项完成后复跑对应测试

## 任务 5：收尾（[AI]）

- [x] 全量验证：fmt / check（all-targets 0 警告）/ test（双 crate，217+16 全绿）/ doc
- [x] review：调度器接线的事务边界（DB 提交后同步调度器、调度失败不回滚只记日志）、
  防重叠与超时、panic 落库路径
- [x] 冒烟四连：启用任务持续落 job_log（7s 17 条）→ 改表达式实时生效（2s 节奏 → 5s 节奏）→
  停用不再执行（12s total 不变）→ 删除无残留（11s total 不变、日志 0 错误）
- [x] Commit：`feat(job): 定时任务调度与执行日志（W6-2）` + 打卡

---

## 验收标准

- 新增测试全红 → 全绿；两 crate 全量测试通过；所有构建目标 0 警告
- 冒烟四连通过；示例任务上线即在调度器中运行
