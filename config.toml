[server]
host = "0.0.0.0"
port = 8080

[database]
# charset=utf8mb4 必须：否则中文（菜单名/昵称等）经 sqlx 读取会乱码
url = "mysql://root:root@localhost:3307/salvo_vben?charset=utf8mb4"

[jwt]
# 开发密钥；生产环境务必用 config.local.toml 或环境变量覆盖
secret = "dev-secret-change-me"
ttl_seconds = 7200
